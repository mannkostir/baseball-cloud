import AuthView from '@/components/AuthView';
import React from 'react';
import { Container } from './Auth.styles';

const Auth = () => {
  return (
    <Container>
      <AuthView />
    </Container>
  );
};

export default Auth;
